# FastAPI web application dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
jinja2>=3.1.2
python-multipart>=0.0.6
pydantic>=2.5.0

# Data processing and visualization (existing dependencies)
matplotlib>=3.8.0
numpy>=1.26.0
python-dateutil>=2.9.0
pygal>=3.0.4

# Optional: Legacy Flask support (can be removed if not needed)
Flask>=3.0.0

# Development and testing dependencies
pytest>=7.4.0
pytest-asyncio>=0.21.0
